<view>
    <!--  3个火枪手  -->
    <view class='btns'>

        <view wx:for="{{btns}}" wx:for-index="index" wx:for-item="item" bindtap='btnTypeTap' class='{{btnType == index ? "selectedBtn" : "btn"}}' data-btnType="{{index}}">{{item}}</view>

    </view>
    <scroll-view class='scrollView'>
        {{btns[btnType]}}
    </scroll-view>
</view>